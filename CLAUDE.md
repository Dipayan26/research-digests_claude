# research-digests_claude — Project Instructions

## Git Push Policy

Always push directly to `main`:

```
git push -u origin HEAD:main
```

Never push to a feature branch or any branch other than `main` unless the user explicitly requests it.
